import type { Config } from 'tailwindcss';

const config: Config = {
  darkMode: false, 

  theme: {
    extend: {
      blur: {
        xs: '2px',
      },
    },
  },

  plugins: [],
};

export default config;
